apiVersion: v1
kind: Service
metadata:
  name: {{ include "slack-steam-status.fullname" . }}
  labels:
    {{- include "slack-steam-status.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slack-steam-status.selectorLabels" . | nindent 4 }}
